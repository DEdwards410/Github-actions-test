name: Shell commands
  
on: [push]

jobs: 
  run-shell-command:
    runs on: ubuntu-latest
    steps: 
     - name: echo a string
       run: echo "Hello World I am great"
     - name: Multiline script running below 
       run: | 
         node -v
         npm -v
         